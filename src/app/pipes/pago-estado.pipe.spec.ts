import { PagoEstadoPipe } from './pago-estado.pipe';

describe('PagoEstadoPipe', () => {
  it('create an instance', () => {
    const pipe = new PagoEstadoPipe();
    expect(pipe).toBeTruthy();
  });
});
